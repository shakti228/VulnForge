[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vulnforge"
version = "0.1.0"
description = "Security Research & Analysis Platform"
requires-python = ">=3.10"
authors = [
    {name = "VYZENTRA"}
]

[project.scripts]
vulnforge = "vulnforge.cli:main"

